pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'master', url: 'https://github.com/Ajinkyapatil1234/website.git'
            }
        }
        stage('Build Docker Image') {
            steps {
                sh 'sudo apt-get update'
                sh 'sudo DEBIAN_FRONTEND=noninteractive apt-get install -y docker.io'
                sh 'sudo docker build -t webapp .'
            }
        }
        stage('Run Container') {
            steps {
                sh 'echo $PATH'
                sh 'which docker'
                sh 'docker --version'
                // Remove existing container if it exists
                sh 'sudo /usr/bin/docker rm -f webapp-container || true'
                sh 'sudo /usr/bin/docker run -d -p 82:80 --name webapp-container webapp'
            }
        }
        stage('Publish') {
            when {
                branch 'master'
            }
            steps {
                sh 'sudo service apache2 start'
                sh 'sudo cp -R * /var/www/html/'
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}



